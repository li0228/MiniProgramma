<!--pages/home.wxml-->
<!--数据绑定-->
<view>{{name}}</view>
<!-- 列表显示 -->
<view wx:for="{{students}}">{{item.name}}</view>
<!-- 事件监听 -->
<view>当前计数：{{counter}}</view>
<button size="mini" bindtap="addCounter">+</button>